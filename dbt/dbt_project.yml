name: nyc_taxi_platform
version: '1.0'

profile: default
target: dev

models:
  nyc_taxi_platform:
    staging:
      +materialized: view
      +schema: staging
    dimensions:
      +materialized: table
      +schema: dimensions
    facts:
      +materialized: table
      +schema: facts
    marts:
      +materialized: table
      +schema: marts
